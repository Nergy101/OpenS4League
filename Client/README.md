# Client

Reserved for the S4 League **client** (the 2013 Windows binary `S4ClientLocal.exe` / exposed data).

## Status

- [ ] Add the client data here (e.g. the unpacked `Standalone Server+Client Season 8 (EU v1267)`
      client used by the server stack).
- [ ] Document the client version (`0.8.32.26995`) and ProudNet version GUID
      (`{beb92241-8333-4117-ab92-9b4af78c688f}`) it expects.

> Kept empty for now by design. The client is **not** part of the .NET 10 server rebuild — it
> stays as-is and the upgraded server must keep speaking its protocol.
